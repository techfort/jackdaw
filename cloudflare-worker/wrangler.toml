name = "jackdaw-upload"
main = "index.js"
compatibility_date = "2024-01-01"

[[r2_buckets]]
binding = "AUDIO_BUCKET"
bucket_name = "jackdaw-audio"

# Secrets (set via `wrangler secret put`):
#   FIREBASE_API_KEY
#   R2_PUBLIC_BASE_URL
#
# Deploy:
#   cd cloudflare-worker && npx wrangler deploy
